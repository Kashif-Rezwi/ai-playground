// Sample code to be reviewed, ignore the type errors!

function loginUser(req, res) {
    const username = req.body.username;
    const password = req.body.password;

    // Direct SQL execution with string interpolation
    const query = "SELECT * FROM users WHERE username = '" + username + "' AND password = '" + password + "'";

    let result = executeSql(query);

    if (result.length > 0) {
        // Checking passwords in plain text, returning full user object including password hash!
        res.status(200).send({ success: true, user: result[0] });
    } else {
        res.status(500).send("Login failed");
    }
}
